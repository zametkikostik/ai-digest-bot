{
  "name": "ai-digest-bot-cf",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "init:db": "wrangler d1 execute ai-digest-bot-db --file=schema.sql",
    "logs": "wrangler tail"
  },
  "devDependencies": {
    "wrangler": "^4.68.1"
  }
}
